# Base URL of the backend API. Leave blank for local dev (Vite proxies
# /api to localhost:8000). Set to the Render service URL for production,
# with no trailing slash: https://your-api.onrender.com
VITE_API_BASE_URL=
